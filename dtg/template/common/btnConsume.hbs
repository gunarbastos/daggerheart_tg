<button type="button"
        class="btn-action bi bi-flask-florence-fill"
        data-action="{{action}}"
        data-tooltip="Consume 1 instance of the item"
        {{#if skipTab}}tabindex="-1"{{/if}}></button>