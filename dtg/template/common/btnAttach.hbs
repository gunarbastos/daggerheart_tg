<button type="button"
        class="btn-action bi bi-link-45deg"
        data-action="{{action}}"
        data-tooltip="Attach Item to another Item"
        {{#if skipTab}}tabindex="-1"{{/if}}></button>