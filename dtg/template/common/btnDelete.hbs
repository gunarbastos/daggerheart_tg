<button type="button"
        class="btn-action danger preserve-width bi bi-trash-fill"
        data-action="{{action}}"
        data-tooltip="BEWARE! This action is permanent"
        {{#if skipTab}}tabindex="-1"{{/if}}></button>
