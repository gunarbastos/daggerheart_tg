<section class="actor-list">
    {{#if inCombat}}
        <div class="title">
            <span>Players</span><span class="line" aria-hidden="true"></span>
        </div>
        {{#each players as |player|}}
            {{> CTPlayer actor=player }}
        {{/each}}
    {{/if}}
</section>
